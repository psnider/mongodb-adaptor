SOURCES='package.json mongodb-adaptor.d.ts'
TARGET=node_modules/@sabbatical/mongodb-adaptor
rm -fr ${TARGET}
mkdir -p ${TARGET}
cp ${SOURCES} ${TARGET}
tsc -p .
cp ./mongodb-adaptor.js ${TARGET} 
tsc -p tsconfig.tests.json
