SOURCES='package.json mongoose-adaptor.d.ts'
TARGET=node_modules/@sabbatical/mongoose-adaptor
rm -fr ${TARGET}
mkdir -p ${TARGET}
cp ${SOURCES} ${TARGET}
tsc -p .
cp ./mongoose-adaptor.js ${TARGET} 
tsc -p tsconfig.tests.json
